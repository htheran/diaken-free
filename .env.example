# Django Configuration (Optional)
# This file is OPTIONAL for development. The application works out of the box.
# Copy this file to .env only if you need to customize settings for production.
# DO NOT commit .env to version control!

# Django Secret Key (Optional - has default for development)
# For production, generate a new key with:
# python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
# DJANGO_SECRET_KEY=your-secret-key-here-change-this

# Debug Mode (Optional - defaults to True for development)
# Set to False for production
# DJANGO_DEBUG=False

# Allowed Hosts (Optional - defaults to '*' in development)
# For production, specify your domain/IP (comma-separated)
# Example: DJANGO_ALLOWED_HOSTS=myserver.example.com,10.100.18.80
# DJANGO_ALLOWED_HOSTS=

# CSRF Trusted Origins (Optional)
# For production with HTTPS, specify your domains (comma-separated, with protocol)
# Example: DJANGO_CSRF_TRUSTED_ORIGINS=https://myserver.example.com
# DJANGO_CSRF_TRUSTED_ORIGINS=

# Encryption Key for credentials (Optional - generated automatically if not set)
# For production, generate a key with: python security_fixes/credential_encryption.py generate-key
# ENCRYPTION_KEY=your-encryption-key-here-change-this

# Database Configuration (Optional - defaults to SQLite)
DB_ENGINE=sqlite3
DB_NAME=db.sqlite3
# For PostgreSQL:
# DB_ENGINE=postgresql
# DB_NAME=diaken
# DB_USER=diaken
# DB_PASSWORD=your-db-password
# DB_HOST=localhost
# DB_PORT=5432

# Email Configuration (Optional)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=

# Redis Cache (Optional)
# REDIS_URL=redis://localhost:6379/1

# Sentry DSN (Optional - for error tracking)
# SENTRY_DSN=
